import { NgxIfArrayEmptyDirective } from '../ngx-if-has-elements/ngx-if-has-items.directive';

describe('NgxIfArrayEmptyDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxIfArrayEmptyDirective();
    expect(directive).toBeTruthy();
  });
});
