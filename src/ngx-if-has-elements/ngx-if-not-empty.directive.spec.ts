import { IsArrayNotEmptyDirective } from './ngx-if-has-items.directive';

describe('IsArrayNotEmptyDirective', () => {
  it('should create an instance', () => {
    const directive = new IsArrayNotEmptyDirective();
    expect(directive).toBeTruthy();
  });
});
